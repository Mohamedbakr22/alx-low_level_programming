#include "main.h"

/**
 * mul - multiplies to integers
 * @x: the first number to multiply
 * @y: the second number to multiply
 * Return: the result of multiplacation between x and y
 */

int mul(int x, int y)

{
	return (x * y);
}
